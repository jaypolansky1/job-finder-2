{
  "name": "astro-simple-blog",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev --port 24",
    "start": "astro dev --port 24",
    "build": "astro build",
    "preview": "astro preview --port 24"
  },
  "devDependencies": {
    "astro": "^4.0.0"
  }
}
