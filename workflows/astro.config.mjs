import { defineConfig } from 'astro/config';

export default defineConfig({
  // optional: set your production site URL here
  // site: 'https://your-domain.example',
  devOptions: {
    port: 24
  }
});
